# Django Settings
DEBUG=True
SECRET_KEY=sige-super-secret-dev-key-change-in-production-2026
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (PostgreSQL)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=sige_db
DB_USER=sige_user
DB_PASSWORD=sige_password
DB_HOST=localhost
DB_PORT=5432

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
